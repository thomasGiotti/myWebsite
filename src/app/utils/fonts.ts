// app/fonts.ts (exemple)
import { JetBrains_Mono, Sora } from "next/font/google";

export const jetBrainsMono = JetBrains_Mono({ subsets: ["latin"], weight: ["700"] });
export const sora = Sora({ subsets: ["latin"], weight: ["600"] });